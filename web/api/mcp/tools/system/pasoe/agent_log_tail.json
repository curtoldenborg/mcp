{
  "name": "agent_log_tail",
  "description": "Returns the last N lines of the PASOE agent log (oepas1.agent.YYYY-MM-DD.log) as a JSON array. Each line contains timestamp, pid, tid, agent id, session id, level and message. Used by the PASOE Agent Log Monitor web page.",
  "inputSchema": {
    "type": "object",
    "properties": {"lines": {"type": "number", "description": "Number of tail lines to return (default 100, max 200)"},"log_date": {"type": "string", "description": "Date yyyy-mm-dd, defaults to today"}},
    "required": []
  }
}