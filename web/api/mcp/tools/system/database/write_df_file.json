{
  "name": "write_df_file",
  "description": "Writes a file to the df_files folder. Used to stage .df files before loading them into the database.",
  "inputSchema": {
    "type": "object",
    "properties": {"file_name": {"type": "string", "description": "Filename to write e.g. scheduler.df"},"content": {"type": "string", "description": "Full file content to write"}},
    "required": ["file_name","content"]
  }
}