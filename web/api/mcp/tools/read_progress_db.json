{
  "name": "read_progress_db",
  "description": "Queries the OpenEdge database using a standard ABL WHERE clause.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "tableName": {
        "type": "string",
        "description": "The table name (e.g., 'Customer')"
      },
      "whereClause": {
        "type": "string",
        "description": "The ABL filter (e.g., 'CustNum < 50)'"
      }
    },
    "required": [
      "tableName",
      "whereClause"
    ]
  }
}
